#!/bin/csh
echo '================================================================='
foreach file (*.?cov)
        echo $file
        grep "#####" $file
        echo '================================================================='
        end
